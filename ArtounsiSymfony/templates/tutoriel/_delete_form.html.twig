<form method="post" action="{{ path('app_tutoriel_delete', {'id_tutoriel': tutoriel.id_tutoriel}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tutoriel.id_tutoriel) }}">
    <button class="btn">Delete</button>
</form>
