<form method="post" action="{{ path('app_demandetravail_delete', {'idDemande': demandetravail.idDemande}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demandetravail.idDemande) }}">
    <button class="btn">Delete</button>
</form>
