<form method="post" action="{{ path('app_offretravailarchive_delete', {'idoffre': offretravailarchive.idoffre}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offretravailarchive.idoffre) }}">
    <button class="btn">Delete</button>
</form>
