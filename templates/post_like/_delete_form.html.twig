<form method="post" action="{{ path('app_post_like_delete', {'id_like': post_like.id_like}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post_like.id_like) }}">
    <button class="btn">Delete</button>
</form>
