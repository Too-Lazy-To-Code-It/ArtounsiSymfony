{% extends 'base.html.twig' %}

{% block title %}Video{% endblock %}

{% block body %}
    <h1>Video</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_video</th>
                <td>{{ video.idVideo }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ video.title }}</td>
            </tr>
            <tr>
                <th>Date_p</th>
                <td>{{ video.dateP ? video.dateP|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ video.description }}</td>
            </tr>
            <tr>
                <th>Pathvideo</th>
                <td>{{ video.pathvideo }}</td>
            </tr>
            <tr>
                <th>Pathimage</th>
                <td>{{ video.pathimage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_video_index') }}">back to list</a>

    <a href="{{ path('app_video_edit', {'id_video': video.id_video}) }}">edit</a>

    {{ include('video/_delete_form.html.twig') }}
{% endblock %}
